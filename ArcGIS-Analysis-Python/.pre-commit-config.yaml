repos:
  - repo: https://github.com/john-f-kennedy-noaa/DisMAP/tree/ncei_dev
    rev: v4.6.0  
    hooks:
      - id: check-yaml          # Validates YAML syntax
      - id: end-of-file-fixer   # Ensures files end with a newline
      - id: trailing-whitespace # Trims trailing spaces
